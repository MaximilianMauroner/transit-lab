// The repository-level package is the source of truth. This local entry point
// keeps Studio imports inside the application boundary without copying the
// contract implementation into the client.
export * from "../../../../../packages/contracts/src/index.js";
